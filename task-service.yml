spring:
  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: http://localhost:8080/realms/Mini-Project
  application:
    name: task-service
  datasource:
    url: jdbc:postgresql://localhost:5432/mini_project_db
    username: postgres
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  api-docs:
    path: /task-service/v3/api-docs
  swagger-ui:
    path: /task-service/swagger-ui.html
    oauth:
      client-id: Mini-Project
      client-secret: ZUBxXkJYEMkNWZDhBf6dEyua90e1QdpK

